import React from 'react';
import { connect } from 'react-redux';

const Photos = (state) => {

  const photos = props.entities.photos.map((image) => {

  });
    return (
      <div>
        <ul>
          <li>{}</li>
        </ul>
      </div>
    );
  };